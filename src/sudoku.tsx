import React, { useState } from "react";

type SudokuCell = {
  Value: string;
  isInValid?: boolean;
  isDefault?: boolean;
};

const Sudoku: React.FC = () => {
  return <div></div>;
};

export default Sudoku;
